/*
** EPITECH PROJECT, 2022
** server
** File description:
** set_time_unit.c
*/

#include "server.h"

void set_time_unit(list_args_t* args)
{
    // char response[MAX_BUFFER] = {0};

    // sprintf(response, "sst %d\n", args->time);

    // append_to_player_write_buffer(args->client, response);
}
